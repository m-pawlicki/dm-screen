[project]
name = "dm-screen"
version = "0.1.0"
description = "CLI application for creating character sheets for Dungeons & Dragons"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "npyscreen>=5.0.2",
]
